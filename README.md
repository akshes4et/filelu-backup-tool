# FileLu Auto Backup Tool

A lightweight Python tool to automate folder uploads to FileLu.

## Features
- Upload entire folders
- Simple command-line usage
- Supports automation

## Setup

Install dependencies:
pip install -r requirements.txt

## Usage

python backup.py --folder ./your_folder --api-key YOUR_KEY
